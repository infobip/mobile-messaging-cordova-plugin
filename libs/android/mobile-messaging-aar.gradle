def excludeAndroidSupportLibs = cdvHelpers.getConfigPreference("IB_ANDROID_EXCLUDE_SUPPORT_LIBS", "false").toBoolean()
def excludeGmsLibs = cdvHelpers.getConfigPreference("IB_ANDROID_EXCLUDE_GMS_LIBS", "false").toBoolean()

if (!excludeAndroidSupportLibs) {
    repositories {
        maven {
            url 'https://maven.google.com/'
            name 'Google'
        }
    }
}

dependencies {
    def mmVersion = '1.9.7'
    compile "org.infobip.mobile.messaging.api:infobip-mobile-messaging-android-interactive-sdk:$mmVersion@aar"
    compile "org.infobip.mobile.messaging.api:infobip-mobile-messaging-android-geo-sdk:$mmVersion@aar"
    compile("org.infobip.mobile.messaging.api:infobip-mobile-messaging-android-sdk:$mmVersion@aar") {
        transitive = true; 
        if (excludeAndroidSupportLibs) {    
            exclude group: 'com.android.support', module: 'support-v4'
            exclude group: 'com.android.support', module: 'appcompat-v7'
        }
        if (excludeGmsLibs) {
            exclude group: 'com.google.android.gms', module: 'play-services-gcm'
            exclude group: 'com.google.android.gms', module: 'play-services-location'
        }
    }
}